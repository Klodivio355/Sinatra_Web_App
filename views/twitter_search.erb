<!DOCTYPE html>
<html>
    <head>
        <link rel="stylesheet" type="text/css" href="css/searchpage.css">
        <script src="js/table_menu.js"></script>
        <title>Incoming Tweets</title>
        <meta http-equiv="refresh" content="120">
    </head>
    <body>
        <h1>Administrator System</h1>
        
        <form method="get">
            Search:<input type="text" name="search" value="<%=h params[:search] %>"/>
            <input type="submit" name="submit" value="Update"/>
        </form>
        <br />
        <% unless @tweets.nil? %>
            <% if @tweets.length == 0 %>
                <p>No tweets found.</p>
            <% else %>
                <br />
                <table border="1">
                    <tr>
                        <th width="120">User</th>
                        <th width="600">Tweet</th>
                        <th>Options</th>
                    </tr>
                    <% @tweets.each do |tweet| %>
                        <tr>
                            <td>
                                <a href="<%= tweet.user.uri %>" target="_blank">
                                    <%= tweet.user.screen_name %>
                                </a>
                            </td>
                            <td>
                                <%= tweet.text %>
                            </td>
                            <td align="center">
                                <div>
                                    <button onClick="showDropdown()" id="dropdownButton">&#9660;</button>
                                    <div id="dropdownMenu" class="dropdownOptions">
                                        <a href="<%= tweet.uri %>">Reply</a>
                                        <a>Warn</a>
                                        <a>Block</a>
                                    </div>
                                </div>
                            </td>
                        </tr>
                    <% end %>
                </table>
            <% end %>
        <% end %>
        <br />
    <%= erb :footer %>